// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: jury_blocage.sql

package gen

import (
	"context"
)

const countPeriodesDeliberees = `-- name: CountPeriodesDeliberees :one
WITH periode_c AS (
    SELECT pe.id FROM public.periode_active pe
    WHERE ($1::text = 'periode' AND pe.id = ANY($2::int[]))
       OR ($1::text = 'option' AND pe.option_id = ANY($2::int[]))
       OR ($1::text = 'promotion' AND pe.option_id IN (
               SELECT o.id FROM public.option_active o WHERE o.promotion_id = ANY($2::int[])))
       OR ($1::text = 'formation' AND pe.option_id IN (
               SELECT o.id FROM public.option_active o WHERE o.promotion_id IN (
                   SELECT p.id FROM public.promotion_active p WHERE p.formation_id = ANY($2::int[]))))
       OR ($1::text = 'unite_enseignement' AND pe.id IN (
               SELECT ue.periode_id FROM public.unite_enseignement ue WHERE ue.id = ANY($2::int[])))
       OR ($1::text = 'matiere' AND pe.id IN (
               SELECT ue.periode_id FROM public.matiere m
               JOIN public.unite_enseignement ue ON ue.id = m.unite_enseignement_id
               WHERE m.id = ANY($2::int[])))
       OR ($1::text = 'controle' AND pe.id IN (
               SELECT ue.periode_id FROM public.controle ct
               JOIN public.matiere m ON m.id = ct.matiere_id
               JOIN public.unite_enseignement ue ON ue.id = m.unite_enseignement_id
               WHERE ct.id = ANY($2::int[])))
)
SELECT count(*)::bigint FROM periode_c pe
WHERE EXISTS (SELECT 1 FROM public.jury_result jr WHERE jr.periode_id = pe.id)
`

type CountPeriodesDelibereesParams struct {
	Perimetre string  `json:"perimetre"`
	Ids       []int32 `json:"ids"`
}

// Périodes délibérées qu'une suppression atteindrait — l'unique définition
// de « couvert par un jury délibéré » (lot correction-blocage-jury, 17
// septembre 2026) : une période active portant au moins un résultat de jury.
// Le périmètre nomme le type des identifiants fournis ; la requête remonte
// (option, promotion, formation) ou descend (UE, matière, contrôle) jusqu'aux
// périodes actives que la cascade toucherait, par les vues actives comme les
// analyses d'impact. Compte = périodes distinctes, c'est ce que le message
// `blocage.jury_delibere` annonce, quel que soit le point d'entrée.
func (q *Queries) CountPeriodesDeliberees(ctx context.Context, arg CountPeriodesDelibereesParams) (int64, error) {
	row := q.db.QueryRow(ctx, countPeriodesDeliberees, arg.Perimetre, arg.Ids)
	var column_1 int64
	err := row.Scan(&column_1)
	return column_1, err
}
