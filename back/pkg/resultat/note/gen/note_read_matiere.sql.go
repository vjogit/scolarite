// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: note_read_matiere.sql

package gen

import (
	"context"
)

const fetchNotesByMatiereID = `-- name: FetchNotesByMatiereID :many
WITH context_rules AS (
    -- Règles de la promotion dont dépend la matière. Seul echelle est utilisé
    -- ici, pour le seuil « E » attribué à un rattrapage validé.
    SELECT prom.echelle
    FROM public.matiere m
    JOIN public.unite_enseignement ue ON ue.id   = m.unite_enseignement_id
    JOIN public.periode_active p             ON p.id    = ue.periode_id
    JOIN public.option_active o              ON o.id    = p.option_id
    JOIN public.promotion_active prom        ON prom.id = o.promotion_id
    WHERE m.id = $1
    LIMIT 1
),
notes_groupees AS (
    SELECT
        u.id AS user_id,
        u."firstName",
        u."lastName",
        m.id AS matiere_id,
        m.name AS matiere_name,

        -- 1. Calcul de la moyenne normale (Session 1), hors notes non évaluées
        (SUM(n.note * c.coeff) FILTER (WHERE c.is_rattrapage = FALSE AND n.not_evaluated = FALSE) /
         NULLIF(SUM(c.coeff) FILTER (WHERE c.is_rattrapage = FALSE AND n.not_evaluated = FALSE), 0))::float AS moyenne_s1,

        -- 2. Comptage des rattrapages validés (Session 2)
        COUNT(n.id) FILTER (WHERE c.is_rattrapage = TRUE AND n.is_validated = TRUE) AS nb_rattrapages_valides,

        -- 3. Détection d'au moins une note non évaluée sur les contrôles normaux
        BOOL_OR(n.not_evaluated) FILTER (WHERE c.is_rattrapage = FALSE) AS has_not_evaluated

    FROM public."user" u
    JOIN public.note n ON u.id = n.user_id
    JOIN public.controle c ON n.controle_id = c.id
    JOIN public.matiere m ON c.matiere_id = m.id
    WHERE m.id = $1
      AND (
          (c.is_rattrapage = FALSE AND (n.note IS NOT NULL OR n.not_evaluated = TRUE)) OR
          (c.is_rattrapage = TRUE AND n.is_validated IS NOT NULL)
      )
    GROUP BY u.id, u."firstName", u."lastName", m.id, m.name
)
SELECT
    ng.user_id,
    ng."firstName",
    ng."lastName",
    ng.matiere_id,
    ng.matiere_name,

    -- 4. Application des règles métier :
    --    - N.E. si au moins un contrôle non évalué
    --    - le seuil « E » de la promotion si un rattrapage est validé
    --    - sinon la moyenne S1
    --
    -- echelle[5] est le dernier seuil de l'échelle, celui qui sépare E de F :
    -- la note plancher d'une validation. Cette valeur était écrite 8.0 en dur,
    -- ce qui ne valait que pour une promotion notée sur 20 dont l'échelle
    -- finit à 8.
    CASE
        WHEN ng.has_not_evaluated          THEN NULL::my_null_float
        WHEN ng.nb_rattrapages_valides > 0 THEN cr.echelle[5]::my_null_float
        ELSE ng.moyenne_s1::my_null_float
    END AS note,

    -- 5. D'où vient la valeur ci-dessus.
    --
    -- Les trois branches produisent un nombre que rien ne distinguait à
    -- l'écran : la note d'un rattrapage validé est un seuil, elle ne
    -- correspond à aucune copie — un élève noté 5 puis rattrapé à 11 s'affiche
    -- 8. Le cas « non évaluée » se déduisait d'un NULL, celui du rattrapage
    -- n'était pas déductible du tout, une moyenne ordinaire pouvant tomber
    -- exactement sur echelle[5]. D'où cette colonne : elle n'entre dans aucun
    -- calcul, elle nomme celui qui a eu lieu.
    --
    -- L'ordre des branches suit celui du calcul, à une addition près :
    -- moyenne_s1 peut être NULL sans qu'aucun contrôle ne soit non évalué —
    -- un rattrapage non validé et aucun contrôle normal noté, par exemple.
    -- La valeur est alors absente elle aussi, et l'annoncer « moyenne »
    -- promettrait un nombre qui n'existe pas.
    CASE
        WHEN ng.has_not_evaluated          THEN 'non_evaluee'
        WHEN ng.nb_rattrapages_valides > 0 THEN 'rattrapage'
        WHEN ng.moyenne_s1 IS NULL         THEN 'non_evaluee'
        ELSE 'moyenne'
    END AS provenance

FROM notes_groupees ng
CROSS JOIN context_rules cr
ORDER BY "lastName", "firstName"
`

type FetchNotesByMatiereIDRow struct {
	UserID      int32    `json:"user_id"`
	FirstName   *string  `json:"firstName"`
	LastName    *string  `json:"lastName"`
	MatiereID   int32    `json:"matiere_id"`
	MatiereName string   `json:"matiere_name"`
	Note        *float64 `json:"note"`
	Provenance  string   `json:"provenance"`
}

func (q *Queries) FetchNotesByMatiereID(ctx context.Context, matiereID int32) ([]FetchNotesByMatiereIDRow, error) {
	rows, err := q.db.Query(ctx, fetchNotesByMatiereID, matiereID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []FetchNotesByMatiereIDRow
	for rows.Next() {
		var i FetchNotesByMatiereIDRow
		if err := rows.Scan(
			&i.UserID,
			&i.FirstName,
			&i.LastName,
			&i.MatiereID,
			&i.MatiereName,
			&i.Note,
			&i.Provenance,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
