/**
 * Formats partagés côté affichage. Centralisés pour que la même règle
 * typographique s'applique à la modale de suppression et aux notifications.
 */

/** Séparateur de milliers français : « 1 847 ». */
export const formatNombre = new Intl.NumberFormat('fr-FR');
